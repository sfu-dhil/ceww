{% extends 'base.html.twig' %}

{% block pageheader %} 
    <h1>DateYear List</h1>
    <p class="count">
        Displaying {{ dateYears|length }} dateYears of {{ dateYears.getTotalItemCount }} total.
    </p>
{% endblock %} 

{% block body %}

    <table class="table table-bordered table-condensed table-hover table-striped">
        <thead>
            <tr>
                <th>Date</th>
                <th>Start</th>
                <th>Startcirca</th>
                <th>End</th>
                <th>Endcirca</th>
            </tr>
        </thead>
        <tbody>
            {% for dateYear in dateYears %}
                <tr>
                    <td>
                        <a href="{{ path('date_year_show', { 'id': dateYear.id }) }}">
                            {{ dateYear }}
                        </a>
                    </td>
                    <td>{{ dateYear.start }}</td>
                    <td>{% if dateYear.startCirca %}Yes{% else %}No{% endif %}</td>
                    <td>{{ dateYear.end }}</td>
                    <td>{% if dateYear.endCirca %}Yes{% else %}No{% endif %}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(dateYears) }}
    </div>

{% endblock %}
