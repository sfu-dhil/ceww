{% extends 'base.html.twig' %}

{% block pageheader %} 
    <h1>Residence Add</h1>
    <p>Use the form below to search for residences by name and add them to {{ author.fullname }} ({{ author.birthDate }}-{{author.deathDate}}).</p>
    <p>Current residences: {{ author.residences|join(',')}}</p>
{% endblock %} 

{% block body %}

    <form method="get" action="{{ path('author_residence_add', {'id': author.id}) }}" class="form-horizontal">
        <fieldset>
            <legend>Search</legend>
            <div class='form-group'>
                <label class='col-sm-2 control-label' for='q'>Search term</label>
                <div class='col-sm-10'>
                    <div class='input-group'>
                        <input type='text' name='q' id='q' class='form-control' value='{{ q }}'>
                        <span class="input-group-btn">
                            <button class="btn btn-primary" type="submit">
                                <span class='glyphicon glyphicon-search'></span>
                                Search
                            </button>
                        </span>
                    </div>
                </div>
            </div>
        </fieldset>
    </form>

    {% if residences|length %}
        <form method="post" action="{{ path('author_residence_add', {'id': author.id}) }}" class="form-horizontal">
            <fieldset>
                <legend>Residences</legend>

                {% for a in residences %}
                    <div class="form-group">
                        <div class="col-sm-offset-2 col-sm-10">
                            <div class="checkbox">
                                <label>
                                    <input type="checkbox" name="residence_id[]" value="{{a.id}}">
                                    {{ a.name }}
                                </label>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </fieldset>
            <div class="form-group">
                <div class="col-sm-2"></div>
                <div class="col-sm-10">
                    <input type="submit" value="Update" class="btn btn-primary" />
                    <a href="{{ path('author_show', { 'id': author.id }) }}" class="btn btn-warning">Cancel</a>
                </div>
            </div>
        </form>
    {% endif %}

{% endblock %}
